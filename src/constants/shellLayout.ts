export const APP_SHELL_ROOT_CLASS = 'app-shell'
export const MAIN_CONTENT_ID = 'main-content'
export const MAIN_CONTENT_SELECTOR = `#${MAIN_CONTENT_ID}`

/** Same threshold in JS and CSS — overlay below, push from this width up. */
export const PUSH_LAYOUT_MIN_PX = 1024
export const PUSH_LAYOUT_MQ = `(min-width: ${PUSH_LAYOUT_MIN_PX}px)`

/** Default expanded rail on XL+ (Tailwind xl). */
export const XL_EXPANDED_MIN_PX = 1280
export const XL_EXPANDED_MQ = `(min-width: ${XL_EXPANDED_MIN_PX}px)`

export const NAVBAR_WIDE_MIN_PX = 768
export const NAVBAR_WIDE_MQ = `(min-width: ${NAVBAR_WIDE_MIN_PX}px)`

export const SIDE_NAV_MIN_WIDTH = '4rem'
export const SIDE_NAV_MAX_WIDTH = '256px'

export const LOCAL_STORE_KEY = 'wcag-allyant-tickets-v1'
